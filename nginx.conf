server {
    listen 80;
    server_name 13.48.226.13 lonestarpediatrics.com www.lonestarpediatrics.com _;

    client_max_body_size 100M;

    root /var/www/patient;
    index index.html;

    location ~* \.mjs$ {
        default_type application/javascript;
        try_files $uri =404;
    }

    location /assets/ {
        try_files $uri =404;
    }

    location /api/ {
        proxy_pass http://127.0.0.1:4000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}

server {
    listen 3001;
    server_name 13.48.226.13 admin.lonestarpediatrics.com _;

    client_max_body_size 100M;

    root /var/www/admin;
    index index.html;

    location ~* \.mjs$ {
        default_type application/javascript;
        try_files $uri =404;
    }

    location /assets/ {
        try_files $uri =404;
    }

    location /api/ {
        proxy_pass http://127.0.0.1:4000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
